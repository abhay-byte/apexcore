# Plan Review: T13-remediation-P0-P1 — PASS 1 ITER 1

## Verdict: REVISE
## Counts: CRITICAL 2 MAJOR 5 MINOR 2 SUGGESTIONS 1

### Findings
#### [CRITICAL] ENUM UI wiring incomplete — TuneRow branching hides enable toggle and slider claim is false
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:50-51,240-262` / `app/src/main/kotlin/com/ivarna/apexcore/ui/iron/tune/TuningRoom.kt:27-35,240-263` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneSpecs.kt:40-46,114-120,250-256`
- Problem: Step 1 extends `TuneOptionUi` with `kind/enumOptions/selectedEnum/onEnumSelect` and says `TuneRow` should branch `if (kind==ENUM) EnumSelector else MachinedToggle` and "Slider already exists for I/O read-ahead etc., keep." Evidence shows `TuningRoom.kt:240-263` currently renders only `MachinedToggle` and has no `Slider`; `TuneSpecs` defines 8 SLIDER specs (GPU_ADRENO 0..3, CPU_UCLAMP, CPU_STUNE, INPUT_BOOST_MS, DEVFREQ_BOOST, VM_* , IO_READAHEAD) that would all be mis-rendered as toggles. For ENUM kinds (CPU_GOVERNOR, GPU_GOVERNOR, THERMAL_SCONFIG, IO_SCHEDULER, NET_TCP) the spec uses `TuneControlKind.ENUM` with intent `TuneValue(on, raw)` where `on` = enabled and `raw` = selected token, but branching removes the toggle so there is no path to set `on=false` for an ENUM. Step 2's `onEnumSelect = { token -> TuneValue(true, token) }` only enables, never disables.
- Evidence: `TuningRoom.kt:27-35` has `checked/onToggle` only; `TuneRow:261` is `MachinedToggle(opt.checked, …)`; no slider code; `TuneSpecs.kt:118` CPU_GOVERNOR `kind=ENUM`, `TuneModels.kt:115-118` `TuneValue(on, raw)`; plan snippet `TuneOptionUi` shows `checked/onToggle` retained but row branching discards it for ENUM.
- Impact: Worker must invent whether ENUM rows show toggle+selector or selector-only, and must design slider UI not in plan; will mis-render SLIDER options or block disable of governors.
- Required planner change: Amend Step 1 to (a) correct slider statement — add explicit `SliderRow` branch using existing `RamModeUi`/`MachinedToggle` pattern or state "SLIDER deferred, out of T13 scope but current toggle is tech debt", (b) define ENUM row as `Row { Text; EnumSelector(selectedEnum, options, enabled=checked) + MachinedToggle(checked) }` or similar, and specify `onToggle` for ENUM disables (sets `TuneValue(false, raw)` retaining last raw), (c) make `kind` field default `SWITCH` to keep backward compat: `val kind: TuneControlKind = SWITCH`. Update Change Map to list `TuneSpecs.kt` if slider added, or explicitly scope it out.

#### [CRITICAL] Preset per-component truth design contradicts `TuneManager.applyForSession` aggregation
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:84-93,140-143` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TunePresetManager.kt:19-59` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneManager.kt:166-242,245-268`
- Problem: `TunePresetManager.applyMaximumPerformance` currently does `ids.map { setIntent }` then `val report = manager.applyForSession(gamePackage)` where `applyForSession` loops `TuneSpecs.all` (all enabled intents, not just preset ids) and aggregates raw write counts (`applied += count` where `CpuFrequencyLockController.apply` returns `policies.size*2`, etc.). Plan Step 5 says "track Map<TuneId,Boolean> from each primitive apply … Do not use … return count; rather treat whole primitive as single component verified iff controller returns >0" and "Do not use coerceAtMost / mapIndexed". It proposes `applied = components.count { it.verified }` but still calls `manager.applyForSession` whose `applied` is raw counts (e.g., 8 for 4 CPU policies). No change to `TuneManager.applyForSession` counting is specified, and alternative "call controllers directly" would bypass session mutex/thermal/start logic and bypass ownership. Worker cannot know whether to (i) change `TuneManager.applyForSession` to return per-component, (ii) make preset call `applier.applyBundle` directly, or (iii) add new `TuneApplier.applyBundleVerifiedBoolean` helper. Ambiguity guarantees falsified reporting remains or session semantics break.
- Evidence: `TunePresetManager.kt:48-58` `coerceAtMost` + `mapIndexed`; `TuneManager.kt:222-224` `val count = applier.applyBundle(id,intent,tier); if(count>0) applied+=count`; `CpuFrequencyLockController.kt:22-42` returns `policies.size*2`; `GpuFrequencyLockController.kt:17-34` returns 2.
- Impact: Attempting smallest change per plan will either keep raw-count over-reporting (still falsified) or break transactional session (if bypassing `applyForSession`). Release will still show wrong `3/5` etc.
- Required planner change: Specify single authoritative fix: e.g., (a) keep `applyForSession` call but add `fun applyForSessionPerComponent(...): Map<TuneId, MutationResult>` or change `TuneApplyReport` to include `components: Map<TuneId,Boolean>` and update its `applied` to `components.count { it.verified }` with migration, and update `TunePresetManager` to derive `components` from that report filtered to `ids`; (b) state that preset must not call controllers directly; (c) update File-Level Change Map to include `TuneManager.kt` + `TuneModels.kt` for new report field, and acceptance criteria to check `report.components`.

#### [MAJOR] Settings verified readback namespace and rollback not fully specified
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:61-68,140` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneApplier.kt:511-623` / `app/src/main/kotlin/com/ivarna/apexcore/fps/privilege/ShellGateway.kt:120-186`
- Problem: Step 3 validates `heads_up` via `Settings.Global.getInt(... "heads_up_notifications_enabled")` (correct namespace) and `policy_control` via `Settings.Global.getString("policy_control")` (correct), but `peak_refresh_rate`/`min_refresh_rate` and `refresh_rate_mode`/`miui_refresh_rate` are read via `Settings.System` then `Global`/`Secure` fallback. Shell writes are always `settings put system …`. On AOSP, reading `System` after shell `put system` is consistent via ContentResolver, but OEMs may shadow in `Secure` or require `settings get system` fallback; plan mentions fallback to `shell settings get` but code snippet shows only `Settings.*` calls, not shell fallback, and does not specify timeout or that `snapshotStore.recordOriginal` must be removed on verification failure (rollback). `TuneApplier.kt:572-578` currently records original before write and never removes on failure; plan snippet says "if mismatch return false + rollback snapshot" but Change Map only says `recordVerified` on success, not removal on failure, and does not handle `restoreFocus*/restoreDisplay*` leak.
- Evidence: `TuneApplier.kt:516` `recordOriginal` then `shell.execute … settings put global …` + `isSuccess`; `557-578` System peek read but all immediate `isSuccess`; no readback.
- Impact: Worker may implement readback via only one namespace and leave stale `settings://` snapshots on failed verify, causing orphan restore later.
- Required planner change: In Step 3 explicitly state: for each Settings apply, (i) read original via `Settings.*` triad (`System`->`Global`->`Secure`) AND `shell execute("settings get …")` as fallback if ContentResolver null, (ii) on `verify==false` call `snapshotStore.removeOriginal(settingsKey)` and return false without `recordVerified`, (iii) on verify true `recordVerified(settingsKey, requested, SETTINGS_VALUE)`, (iv) apply same to restores (verify restore readback then remove). Add that to `TuneApplier.kt` change row.

#### [MAJOR] Snapshot verification mode first-write storage API unspecified
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:107-114,144` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneApplier.kt:286` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneSnapshotStore.kt:28-63,132-174`
- Problem: `applySingleNode` does `snapshotStore.recordOriginal(node.path, tokenToSnapshot)` (legacy overload without verificationMode, defaults to EXACT_STRING in `TuneModels.kt:132-137` logic and `TuneSnapshotStore.kt:31` stores `EXACT_STRING`). It then does `shell.write(..., verificationMode=node.verificationMode)` and never calls `recordVerified`. For `IO_SCHEDULER` the correct mode is `IO_SCHEDULER_ACTIVE_TOKEN`, not `GOVERNOR_TOKEN`/`EXACT_STRING`. Step 9 says "Update legacy recordOriginal call to also store verificationMode = node.verificationMode on first insert (use recordOriginal overload or new helper)" and "After writeRes.verified true … recordVerified", but does not define which overload to change (`recordOriginal(path, value, owner, txn, backend)` hardcodes EXACT_STRING) nor that call site 286 must pass `node.verificationMode`. Without that, restore `TuneApplier.kt:132,178` reads `getEntry(path)?.verificationMode ?: verificationModeFor(original)` and will use wrong mode for migrated snapshots, failing verification on restore.
- Evidence: `TuneSnapshotStore.kt:29-34` legacy stores `EXACT_STRING`; `45-55` owner overload also hardcodes `EXACT_STRING`; `58-63` `recordVerified` requires entry exists; `TuneApplier.kt:286` calls one-arg overload; `LoadSnapshot` correctly parses `verificationMode` name but old entries lack it.
- Impact: Restores of IO_SCHEDULER (and any ENUM) after update will use wrong comparator, appearing as WRITE_NOT_EFFECTIVE and leaking snapshots.
- Required planner change: Amend Step 9 / Change Map for `TuneSnapshotStore.kt` to (a) add `fun recordOriginal(path, value, verificationMode)` or extend existing overload to take `verificationMode` param (default `EXACT_STRING` for compat), (b) change `TuneApplier.kt:286` to `recordOriginal(path, tokenToSnapshot, node.verificationMode)` or to owner-aware overload with mode, then `if(writeRes.verified) recordVerified(path, targetValue, node.verificationMode)`, (c) note migration: existing JSON entries without `verificationMode` keep `EXACT_STRING` until next successful write repairs them (already documented, keep).

#### [MAJOR] Capability refresh race fix leaves concurrency, cache TTL, and StateFlow sync unspecified
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:116-127,139-141` / `app/src/main/kotlin/com/ivarna/apexcore/ui/shell/MainScreen.kt:222-251` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneManager.kt:84-86` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneProbe.kt:61-78`
- Problem: `MainScreen.refreshTune()` does `isTuneProbing=true; tuneManager.refreshCapabilities(); val caps = tuneManager.capabilities.value` — race because `refreshCapabilities()` returns immediately and launches `probeJob`. Plan Step 10 proposes `probeSync()`/`refreshCapabilitiesSync()` but `TuneProbe.probeSync()` bypasses `CACHE_TTL_MS`/`lastBackendFingerprint` check (`refreshCapabilities` does TTL short-circuit, `probeSync` does not) and always runs `probeInternal()` on `Dispatchers.IO`. It also does not coordinate `isTuneProbing` StateFlow vs `MainScreen.isTuneProbing` local state. No cancellation of previous `probeJob`, no handling of rapid double-tap on PROBE or `LaunchedEffect` recomposition, and no specification whether `refreshTune` becomes `suspend` or still `scope.launch`. Worker will duplicate probing or miss cache.
- Evidence: `TuneProbe.kt:61-73` TTL check only in `refreshCapabilities`; `75-78` `probeSync` always calls `probeInternal`; `MainScreen.kt:222` `scope.launch { isTuneProbing=true; refreshCapabilities(); val caps = capabilities.value }`.
- Impact: Stale `Checking…` flash not fixed, or probe storm, or cache invalidated too aggressively.
- Required planner change: In Step 10 specify exact helper: `suspend fun TuneManager.refreshCapabilitiesSync(): Map` that delegates to `probe.probeSync()` but respects fingerprint (or explicitly bypasses TTL with reason) and joins existing `probeJob` (`probeJob?.join()` before `probeInternal`). Make `MainScreen.refreshTune` a `suspend` function or keep `scope.launch` but `val caps = tuneManager.refreshCapabilitiesSync()` and set `isTuneProbing` from `tuneManager.probe.isProbing.collect` or local flag with `try/finally`. Add to Change Map `TuneProbe.kt` if TTL handling changed.

#### [MAJOR] Game Mode per-package source is ambiguous (firstOrNull vs selected pkg prop)
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:57,222-251` / `app/src/main/kotlin/com/ivarna/apexcore/tune/GameModeController.kt:18-38,40-69` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneManager.kt:88-92`
- Problem: Step 2 says `val gamePkg = gameManager.load().firstOrNull()?.pkg ?: prefs.getSessionPkg()` **or** pass selected `gamePackage` prop into `TuningRoom`; `gameManager.load()` order is insertion order (or sort undefined) and picks arbitrary first game when user has many. `prefs.getSessionPkg()` is only set after `applyForSession`. For capability display before any session, `firstOrNull` is non-deterministic and not the game user will launch next. `GameModeController.query` is per-package (`cmd game list-modes <pkg>`) so wrong pkg shows wrong available/verified state. Plan leaves two alternatives without choosing, so worker will guess.
- Evidence: `TuneManager.gameModeCapability(gamePackage)` takes explicit pkg; `MainScreen.kt:144-161` `refreshGames` + `GameInfo(pkg, name)`; `TuningRoom` currently has no `selectedGamePackage` param.
- Impact: UI will show Game Mode available when it is not for the game actually launched, or vice versa, leading to false preset reporting.
- Required planner change: Decide single source: add `selectedGamePkg: String?` param to `TuningRoom` and `MainScreen` state `var selectedTuneGamePkg by remember { mutableStateOf<String?>(null) }` defaulting to `gamesList.firstOrNull()?.pkg` with explicit user selection UI (dropdown of games) or `prefs.getSessionPkg()` if non-blank, and pass that pkg to `gameModeCapability`. Remove `firstOrNull` fallback alternative. Update File-Level Change Map to include `GameManager`/`GameInfo` prop plumbing if needed.

#### [MAJOR] Thermal guard live-start must gate on verified count and handle restore path; stop condition races
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:69-83,141` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneManager.kt:101-142,235-237,369-371` / `app/src/main/kotlin/com/ivarna/apexcore/tune/TuneThermalGuard.kt:27-50`
- Problem: Step 4 snippet checks `if (value.on && id.isMaxLock()) { if(thermalGuard.severe) return true; thermalGuard.start() }` but starts guard **before** checking `applier.applyBundle` result and before knowing `count>0`. `TuneThermalGuard.start()` is idempotent (`pollJob != null` return) but starting without verified write leaves guard running with no lock to release, and severe callback will releaseMaxLocks that were never applied. Also `else if (!value.on && id.isMaxLock()) { if (!prefs.getIntent(CPU_LOCK_MAX).on && !prefs.getIntent(GPU_LOCK_MAX).on) stop }` races: `prefs.getIntent` already set to new `value` before check, but `capabilities` may still show unavailable, and `restoreBundle` for that id may still fail (return 0) while guard is stopped prematurely. Plan also says "Also handle restoreBundle for max locks disabling guard when last lock off" without specifying where (inside `setIntent` else branch vs `restoreBundle` suspend).
- Evidence: `TuneManager.kt:110-139` `setIntent` launches `mutex.withLock { if(value.on) { if(severe) Log; else if(GAME…) else applyBundle } else { restoreBundle; if(anyOn==false) restoreSessionLocked } }` with no guard start/stop; `applyForSession:235-237` starts guard only if `applied>0` and intent on; `TuneThermalGuard.start:27` idempotent.
- Impact: Guard may be left running or stopped at wrong time; severe path may log release of 0 locks; battery/heat risk not matching spec §9.
- Required planner change: Refine Step 4 to (a) after `val count = applier.applyBundle(...)` check `if(count>0 && (id==CPU_LOCK_MAX||GPU_LOCK_MAX)) thermalGuard.start()` else do not start, (b) move stop check after successful `restoreBundle` verified: `val restored = applier.restoreBundle(...); val anyMaxOn = prefs.getIntent(CPU_LOCK_MAX).on || prefs.getIntent(GPU_LOCK_MAX).on; if(!anyMaxOn) thermalGuard.stop()` (prefs already updated, but guard stop only if no remaining verified owners — consider `snapshotStore.owners(path)`), (c) document that severe flag is volatile and `start()` is no-op if already started.

#### [MINOR] Change Map lacks file:line citations and omits required `TuneControlKind` default
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:135-148`
- Problem: Review requirement is every P0/P1 maps to file-level change with `file:line` citations. Table lists files but no lines, and omits that `TuneModels.kt:6-10` `TuneControlKind` and `TuneCategory` may need no change, but `TuningRoom.kt:27` change requires `kind` default to `SWITCH` to avoid breaking `MainScreen.kt:234` construction which currently passes no `kind`. Without default, compilation fails.
- Evidence: `TuneOptionUi` construction at `MainScreen.kt:234-244` currently 6 args; new `kind` required breaks it.
- Impact: Worker will hit compile error and guess default.
- Required planner change: Add line citations to each Change Map row (e.g., `TuningRoom.kt:27-35,240-263`, `MainScreen.kt:222-251`, `TuneApplier.kt:511-649,251-312`, `TuneManager.kt:101-142,235-237`) and note `TuneOptionUi.kind` defaults to `TuneControlKind.SWITCH` (or `spec.kind`) for backward compat.

#### [MINOR] Shizuku stdout/stderr split leaves `ShellResult`/`ShellGateway` API unspecified
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:128-130,145` / `app/src/main/aidl/com/ivarna/apexcore/fps/privilege/IPrivilegedExecutor.aidl:6` / `app/src/main/kotlin/com/ivarna/apexcore/fps/privilege/ShizukuUserService.kt:21-23,73-76` / `app/src/main/kotlin/com/ivarna/apexcore/fps/privilege/ShizukuExecutorClient.kt:38-46` / `app/src/main/kotlin/com/ivarna/apexcore/fps/privilege/ShellGateway.kt:154-209`
- Problem: AIDL currently `Bundle execute(String command, long timeoutMs)` with `KEY_OUTPUT` single stream (`redirectErrorStream(true)`). Step 11 says "return separate stdout/stderr/exitCode (parceled ExecutorResult); adapt … to capture streams separately (not redirectErrorStream(true))" and "keep legacy output field for compatibility, add optional stderr field". But `ShellGateway.executeViaShizuku` expects `ShellResult(output, exitCode)` (`ShellResult` has no `stderr`), and `LegacyShizukuProcessExecutor` also returns only `output`. Plan does not specify whether `ShellResult` gains `stderr: String?` or whether `ShellGateway.parseWriteOutput` must be updated to prefer stdout, nor the new Bundle keys (`stdout`/`stderr`). Priority is labeled P2 in plan vs P1 in review, minor inconsistency.
- Evidence: `IPrivilegedExecutor.aidl:6` single Bundle; `ShizukuUserService.kt:22` `redirectErrorStream(true)`; `ShizukuExecutorClient.kt:43-45` `KEY_OUTPUT` only; `ShellGateway.kt:201-207` `executeViaShizuku`.
- Impact: Worker must design Bundle schema and ShellResult migration; risk of breaking `ShellGateway.writePath` which parses `RC=`/`READBACK=` from merged stream—stderr split may hide errors previously in merged output if not handled.
- Required planner change: In Step 11 specify concrete AIDL/Bundle keys (`stdout`, `stderr`, `exitCode`) plus backward-compat fallback (`output` = `stdout + "\n" + stderr` if caller old), and that `ShellResult` adds `stderr: String? = null` field (or `ShellGateway.executeViaShizuku` concatenates only stdout for `ShellResult.output` but logs stderr). Keep `sh -c` unchanged for this step.

### Suggestions
#### [SUGGESTION] Risks missing empty governor intersection and multi-game empty state
- Location: `docs/plans/T13-remediation-P0-P1-plan.md:166-172`
- Problem: Risks list covers compose dropdown complexity, OEM Settings namespace split, thermal listener pre-Q, Shizuku AIDL breaking, existing snapshots wrong mode, but not (a) governor intersection can be empty (no common governor across policies) leading to `EnumSelector` with 0 options, or (b) `GAME_MODE_PERFORMANCE` when `gameManager.load()` empty (no games pinned) — capability query should show "No game selected" rather than `Not available on kernel`.
- Evidence: `TuneProbe.probeCpuGovernor:374-376` `governorIntersection` may be empty; `MainScreen` with empty `gamesList`.
- Impact: Minor UX gap, not blocking correctness.
- Required planner change: Add risk mitigations: (a) if `availableOptions.isEmpty()` show "No common governor" disabled row, (b) if `selectedGamePkg` blank show Game Mode row as `available=false` with reason "Select a game".

## Next Agent: Planner
## Next Action: Revise `docs/plans/T13-remediation-P0-P1-plan.md` to address CRITICAL 2 + MAJOR 5 items above: (1) fix ENUM/SLIDER row spec with toggle+selector and kind default, (2) make preset per-component reporting authoritative via TuneManager report change (not direct controller calls), (3) make Settings verification namespace fallback + snapshot rollback explicit, (4) define snapshot verificationMode first-write API, (5) define refreshCapabilitiesSync concurrency/TTL, (6) choose single Game Mode pkg source + UI prop, (7) gate thermal guard on verified count and clarify restore stop. Re-verify citations file:line and add them to Change Map; keep scope minimal (no unrelated Iron redesign). After revision, re-run plan-reviewer.

