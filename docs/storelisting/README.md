# Store listing assets

## Screenshots

`phoneScreenshots/` and the matching Fastlane copies under
`fastlane/metadata/android/en-US/images/phoneScreenshots/` still show the
**pre–Zen Organic (cryo/tech)** UI.

**Action required after Zen UI lands:** recapture all phone screenshots on a
real device (or emulator) running the Zen Organic theme, then replace both
directories. Do not ship store assets that depict the old dark cryo chrome.

Brand/logo and feature graphic sources for Zen live under `docs/brand/`
(`apexcore_logo_zen*.png`, `featureGraphic_zen.png`) — promote those into
storelisting/fastlane when packaging the listing refresh.
